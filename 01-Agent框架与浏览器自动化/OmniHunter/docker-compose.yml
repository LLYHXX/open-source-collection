services:
  aififteen_hunter:
    build: .
    container_name: aififteen_hunter
    ports:
      - "18800:18800"
    env_file:
      - backend/.env
    volumes:
      - aififteen_hunter_data:/app/data
    restart: unless-stopped

volumes:
  aififteen_hunter_data:
